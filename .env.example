VITE_API_URL=http://localhost:4000/api

# Google Maps JavaScript API (mapa de "Choferes en camino"). Key separada de la del
# backend (esta se expone en el navegador): Google Cloud Console > Credentials >
# Create Credentials > API key > restringir a "Maps JavaScript API" y a los
# dominios/HTTP referrers donde corre la app.
VITE_GOOGLE_MAPS_API_KEY=
