<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Notificaciones push (Area C, exceso de velocidad) - icono por defecto cuando
        la notificacion no trae uno propio, ver usePushNotifications.js. Tiene que ser
        una silueta blanca sobre transparente (ic_stat_notify, generada del logo) - un
        icono a color como ic_launcher se veria como un cuadrado blanco solido, Android
        no admite color en el icono de la barra de estado desde la version 5. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Ubicacion en segundo plano: el chofer debe otorgar "Permitir todo el tiempo"
    para que el tracking siga funcionando con la app minimizada o la pantalla bloqueada. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Notificaciones push - obligatorio declararlo desde Android 13 (API 33), el
    permiso en si se pide en tiempo de ejecucion (ver PushNotifications.requestPermissions
    en usePushNotifications.js). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
